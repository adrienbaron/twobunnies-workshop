const res = {
  bunniesPlist: 'res/sprites/bunnies.plist',
  itemsPlist: 'res/sprites/items.plist',
  grassPng: 'res/sprites/grass.png',
}

const resourcesPathArray = Object.values(res)

export { resourcesPathArray, res }
